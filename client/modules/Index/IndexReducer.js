// Import Actions
import {  } from './IndexActions';

// Initial State
const initialState = {};

const IndexReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default IndexReducer;
